package elements;

import primitives.Color;

public class SpotLight extends PointLight {
    /**
     * @param intensity
     */
    public SpotLight(Color intensity) {
        super(intensity);
    }
}
